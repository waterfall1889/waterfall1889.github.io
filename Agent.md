# Agent.md — 本仓库强制须知

> **每一次任务开始前，必须先阅读本文件全文，再执行任何操作。**  
> 配套规则：`.cursor/rules/notes-migration-freeze.mdc`（`alwaysApply: true`）

---

## 0. 开场强制动作

每个新任务 / 用户请求开始时：

1. **读取** `Agent.md`（本文件）
2. 确认冻结状态仍有效（除非用户本回合已明确说「解除冻结」）
3. 再按用户当次指令行动

未完成本步骤，不得改文件、跑脚本、commit、push。

---

## 1. 项目是什么

- 个人站点仓库：`waterfall1889.github.io` / 工作区 `selfpage`
- 长期笔记在 `src/content/notes/`，按 Course / Tech 分组
- 笔记图片在 `src/content/notes/images/<Group>/<NoteSlug>/`，并镜像到 `public/notes-images/`
- 这是**长期积累的笔记资产**，不是可随意试错的测试数据

---

## 2. 事故记录（必须铭记）

- 事故提交：`5cfaa46`（`0827/reorganize-note-images-per-note`）
- 任务本意：整理 note images
- 实际后果：三份 CSE 笔记正文被截断并提交进 Git
  - RPC：约删除 **89** 行
  - DFS：约删除 **385** 行
  - Consistency：约删除 **177** 行
  - **合计约 651 行**
- 性质：**数据完整性事故**，不是“图片路径小问题”
- 恢复：已从 Git 历史原文恢复并提交（`3bcfb3b` 等）；**禁止再用 AI 凭记忆重写丢失正文**

权威恢复源（备查）：

| 文件 | 事故前 blob |
| --- | --- |
| `Remote-Procedure-call.md` | `150e339` |
| `Distributed file system (DFS).md` | `05bbadc` |
| `Consistency.md` | `5f5927f` |

---

## 3. 脚本冻结（ACTIVE — 直至「解除冻结」）

以下在明确解除前：**禁止执行、禁止调用、禁止自动触发**

### 3.1 点名冻结

- `scripts/migrate-note-images.mjs`
- `scripts/fix-marktext-images.mjs`
- `scripts/convert-docx.mjs`
- `npm run migrate-note-images`
- `npm run fix-note-images`
- `npm run convert-notes`  
  （`package.json` 入口已改为直接 `exit 1`；底层 `.mjs` **保留不删**）

### 3.2 类型冻结

- Markdown / MDX / DOCX 自动转换
- 笔记图片迁移、重命名、目录重组
- 批量修改 Markdown 或图片引用
- 含 `writeFileSync` / `WriteAllText` / `Set-Content` 等写盘的批处理
- 含 `rm` / `rmSync` / `unlink` / 删原文件的脚本
- 任何“迁移 / 整理 / 重构 / 转换 / 自动修复”批处理工具

### 3.3 只读例外

- `scripts/check-note-images.py` 为只读检查，可按需运行；**不得**与写入脚本捆绑自动执行

---

## 4. 严禁自动删除

任何脚本、Agent、自动化流程：

> **不得删除**原始文件、原始图片、原始 Markdown、历史版本或备份。

迁移若将来获准，必须：

**复制 → 验证 → 人工确认 → 才允许删除**

禁止：

**移动/复制 → 自动删除源文件**

---

## 5. 严禁 AI 自动重写原始笔记

对 `src/content/notes/` 下**现有**笔记：

- 不得因格式转换重写全文
- 不得为修图重新生成 Markdown
- 不得“理解后重新整理”正文冒充原文
- 不得在无 diff 审计下覆盖原文件
- 用户要求“严禁润色”时：只提交/只按指令操作，**私自润色零容忍**

任何修改必须：

- 能通过 Git diff 看清
- 范围与任务目标严格一致

丢失内容若需恢复：**只能从 Git blob / 用户提供原文恢复**，禁止模型补写。

---

## 6. 强制 Dry-run

今后任何批量文件修改工具，默认：

**只生成变更预览，不执行写入。**

仅在用户明确确认后才能写盘。执行前必须展示：

- 将修改哪些文件
- 每个文件修改多少处
- 将删除哪些文件
- 将移动/复制哪些文件
- 修改前后路径
- 是否存在潜在覆盖
- 是否存在潜在数据丢失

---

## 7. Git 保护

批量修改或敏感操作前：

1. 检查 `git status`
2. 确认工作区状态
3. 不得覆盖用户已有未提交修改
4. 不得 `git reset --hard`
5. 不得 `git clean -fd`
6. 不得擅自 revert / reset 用户已有提交
7. 不得擅自 commit / push（**仅当用户明确要求时**才可）
8. 发现与任务无关的脏改动：**停止并报告**，不得自行处理
9. **严禁**为修三份笔记而 `revert 5cfaa46` 整提交（仓库范围事故提交，后面已有正常提交）

用户明确要求提交/推送时：按仓库惯例提交，**不做额外润色**。

---

## 8. 异常立即停机

出现任一下列情况，**立即停止整批处理**，不得继续，不得“自动修复”：

- 输出数量异常
- 文件大小异常变化
- Markdown 行数大幅减少
- 内容被截断
- 编码异常
- 图片数量异常
- 路径数量异常
- unexpected deletion
- write error

---

## 9. 笔记整理工作的正确姿态

- 用户长期笔记 ≠ 练手材料
- “图片迁移 / 路径重构”**不是**普通格式化，默认按数据完整性高敏操作处理
- **默认安全策略：不删除、不覆盖、不自动转换、不自动重写**
- 确需上述操作：先报告计划、影响范围、dry-run diff → **等待明确批准** → 再写入
- Application-Architecture 等系列笔记：可按用户要求整理结构，但不得无确认地大范围覆盖；图片路径修复须窄范围、可审查；冻结解除前不得跑迁移脚本

---

## 10. 语言与沟通

- 对用户回复使用 **中文**
- 事故与数据风险相关回复：直接、严肃、不辩解
- 不夸大进度；不确定就停止并询问

---

## 11. 解除冻结

仅当用户明确说出：

> **解除冻结**

之后，仍须：dry-run → 展示影响 → 再获一次明确批准 → 才可对具体脚本/任务解禁执行。  
口头模糊同意不等于解除冻结。

---

**本文件优先于习惯性“帮忙整理一下”的默认行为。冲突时以本文件与用户当次明确指令为准。**
